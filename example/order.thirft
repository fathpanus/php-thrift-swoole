namespace php call.services.order
namespace java call.services.order

struct Order {
    1:i32 orderId,
    2:string remark
}

service OrderService {
    i32 getOrderId()
    Order orderInit(1:i32 orderId)
    bool HasId(1:i32 id)
}